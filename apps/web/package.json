{
  "name": "@sausixudos/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.app.json --noEmit && tsc -p tsconfig.node.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.app.json --noEmit && tsc -p tsconfig.node.json --noEmit",
    "test": "node --import tsx --test src/musicCommandRouting.test.ts src/components/Markdown.test.tsx"
  },
  "dependencies": {
    "@livekit/krisp-noise-filter": "^0.4.4",
    "@sausixudos/shared": "0.1.0",
    "livekit-client": "^2.22.2",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "@vitejs/plugin-react": "^5.1.1",
    "tsx": "^4.20.6",
    "vite": "^8.2.2"
  }
}
